apiVersion: v1
kind: Service
metadata:
  name: {{ include "nextstat-server.fullname" . }}
  labels:
    {{- include "nextstat-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nextstat-server.selectorLabels" . | nindent 4 }}
