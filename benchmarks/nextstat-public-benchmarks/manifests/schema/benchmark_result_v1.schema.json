{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nextstat.io/schemas/benchmarks/benchmark_result_v1.schema.json",
  "title": "NextStat Benchmark Result v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "suite", "case", "deterministic", "environment", "meta", "dataset", "parity", "timing"],
  "properties": {
    "schema_version": { "const": "nextstat.benchmark_result.v1" },
    "suite": { "type": "string", "minLength": 1 },
    "case": { "type": "string", "minLength": 1 },
    "deterministic": { "type": "boolean" },
    "environment": { "type": "object", "additionalProperties": true },
    "meta": {
      "type": "object",
      "additionalProperties": false,
      "required": ["python", "platform", "pyhf_version", "nextstat_version"],
      "properties": {
        "python": { "type": "string" },
        "platform": { "type": "string" },
        "pyhf_version": { "type": "string" },
        "nextstat_version": { "type": "string" }
      }
    },
    "dataset": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "sha256"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "path": { "type": "string" },
        "sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" }
      }
    },
    "model": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "n_main_bins": { "type": "integer", "minimum": 0 },
        "n_params": { "type": "integer", "minimum": 0 }
      }
    },
    "parity": {
      "type": "object",
      "additionalProperties": false,
      "required": ["ok", "nll_pyhf", "nll_nextstat", "abs_diff", "rel_diff", "atol", "rtol"],
      "properties": {
        "ok": { "type": "boolean" },
        "nll_pyhf": { "type": "number" },
        "nll_nextstat": { "type": "number" },
        "abs_diff": { "type": "number", "minimum": 0 },
        "rel_diff": { "type": "number", "minimum": 0 },
        "atol": { "type": "number", "minimum": 0 },
        "rtol": { "type": "number", "minimum": 0 }
      }
    },
    "timing": {
      "type": "object",
      "additionalProperties": false,
      "required": ["nll_time_s_per_call", "speedup_pyhf_over_nextstat"],
      "properties": {
        "nll_time_s_per_call": {
          "type": "object",
          "additionalProperties": false,
          "required": ["pyhf", "nextstat"],
          "properties": {
            "pyhf": { "type": "number", "minimum": 0 },
            "nextstat": { "type": "number", "minimum": 0 }
          }
        },
        "speedup_pyhf_over_nextstat": { "type": "number" }
        ,
        "raw": {
          "type": "object",
          "additionalProperties": false,
          "required": ["number", "repeat", "target_s", "policy", "per_call_s"],
          "properties": {
            "number": { "type": "integer", "minimum": 1 },
            "number_per_engine": {
              "type": "object",
              "additionalProperties": false,
              "required": ["pyhf", "nextstat"],
              "properties": {
                "pyhf": { "type": "integer", "minimum": 1 },
                "nextstat": { "type": "integer", "minimum": 1 }
              }
            },
            "calibration": { "type": "string" },
            "repeat": { "type": "integer", "minimum": 1 },
            "target_s": { "type": "number", "minimum": 0 },
            "policy": { "type": "string", "enum": ["min", "median"] },
            "per_call_s": {
              "type": "object",
              "additionalProperties": false,
              "required": ["pyhf", "nextstat"],
              "properties": {
                "pyhf": { "type": "array", "items": { "type": "number", "minimum": 0 } },
                "nextstat": { "type": "array", "items": { "type": "number", "minimum": 0 } }
              }
            }
          }
        }
      }
    }
    ,
    "fit": {
      "type": "object",
      "additionalProperties": false,
      "required": ["status"],
      "properties": {
        "status": { "type": "string", "enum": ["ok", "skipped", "failed"] },
        "reason": { "type": "string" },
        "time_s": {
          "type": "object",
          "additionalProperties": false,
          "required": ["pyhf", "nextstat"],
          "properties": {
            "pyhf": { "type": "number", "minimum": 0 },
            "nextstat": { "type": "number", "minimum": 0 }
          }
        },
        "speedup_pyhf_over_nextstat": { "type": "number" },
        "raw": {
          "type": "object",
          "additionalProperties": false,
          "required": ["repeat", "policy", "per_fit_s"],
          "properties": {
            "repeat": { "type": "integer", "minimum": 1 },
            "policy": { "type": "string", "enum": ["min", "median"] },
            "per_fit_s": {
              "type": "object",
              "additionalProperties": false,
              "required": ["pyhf", "nextstat"],
              "properties": {
                "pyhf": { "type": "array", "items": { "type": "number", "minimum": 0 } },
                "nextstat": { "type": "array", "items": { "type": "number", "minimum": 0 } }
              }
            }
          }
        },
        "meta": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "poi_name": { "type": "string" },
            "poi_hat_pyhf": { "type": "number" },
            "poi_hat_nextstat": { "type": "number" },
            "nll_pyhf": { "type": "number" },
            "nll_nextstat": { "type": "number" },
            "nextstat_success": { "type": "boolean" },
            "nextstat_converged": { "type": "boolean" },
            "nextstat_n_iter": { "type": "integer", "minimum": 0 },
            "nextstat_n_evaluations": { "type": "integer", "minimum": 0 },
            "nextstat_termination_reason": { "type": "string" }
          }
        },
        "parity": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "abs_diff_nll_at_pyhf_bestfit": { "type": "number", "minimum": 0 },
            "abs_diff_nll_at_nextstat_bestfit": { "type": "number", "minimum": 0 }
          }
        }
      },
      "allOf": [
        {
          "if": { "properties": { "status": { "const": "ok" } }, "required": ["status"] },
          "then": { "required": ["time_s", "speedup_pyhf_over_nextstat", "raw"] }
        }
      ]
    }
  }
}
