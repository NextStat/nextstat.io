{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nextstat.io/schemas/benchmarks/insurance_benchmark_result_v1.schema.json",
  "title": "NextStat Insurance Benchmark Result v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "suite",
    "case",
    "deterministic",
    "status",
    "meta",
    "dataset",
    "config",
    "parity",
    "timing",
    "results"
  ],
  "properties": {
    "schema_version": { "const": "nextstat.insurance_benchmark_result.v1" },
    "suite": { "const": "insurance" },
    "case": { "type": "string", "minLength": 1 },
    "deterministic": { "type": "boolean" },
    "status": { "type": "string", "enum": ["ok", "warn", "failed"] },
    "reason": { "type": ["string", "null"] },
    "meta": {
      "type": "object",
      "additionalProperties": true,
      "required": ["python", "platform"],
      "properties": {
        "python": { "type": "string" },
        "platform": { "type": "string" },
        "nextstat_version": { "type": ["string", "null"] },
        "chainladder_version": { "type": ["string", "null"] }
      }
    },
    "dataset": {
      "type": "object",
      "additionalProperties": true,
      "required": ["id", "sha256"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
        "spec": { "type": "object" }
      }
    },
    "config": { "type": "object", "additionalProperties": true },
    "parity": {
      "type": "object",
      "additionalProperties": true,
      "required": ["status", "reference", "metrics"],
      "properties": {
        "status": { "type": "string", "enum": ["ok", "warn", "skipped"] },
        "reference": {
          "type": "object",
          "additionalProperties": false,
          "required": ["name", "version"],
          "properties": {
            "name": { "type": "string" },
            "version": { "type": "string" }
          }
        },
        "metrics": { "type": "object", "additionalProperties": true }
      }
    },
    "timing": { "type": "object", "additionalProperties": true },
    "results": { "type": "object", "additionalProperties": true }
  }
}

