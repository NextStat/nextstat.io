# ROOT/RooFit Baseline (Template)

This directory is a **template** for optional ROOT/RooFit/RooStats baselines for the HEP suite.

Status: template. The runner emits a machine-readable `skipped` result unless PyROOT is available.

Run:

```bash
python run.py --case root_env_smoke --out out/root_baseline.json
```

